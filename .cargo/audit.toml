# cargo-audit configuration
# See: https://github.com/rustsec/rustsec/tree/main/cargo-audit

[advisories]
# RUSTSEC-2023-0071: rsa crate - Marvin Attack timing sidechannel
# Transitive dep via sqlx-macros-core → sqlx-mysql.
# BANKO uses PostgreSQL only. The rsa crate is pulled in for
# compile-time macro expansion, never used at runtime.
# No upstream fix available as of 2026-04-08.
ignore = ["RUSTSEC-2023-0071"]
